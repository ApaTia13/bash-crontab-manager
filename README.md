# Crontab Manager

Удобный интерактивный менеджер для управления системными заданиями cron в Linux.  
Позволяет безопасно просматривать, добавлять, редактировать и удалять задачи в файле `/etc/crontab` с автоматическим созданием резервных копий и проверкой синтаксиса.

## Возможности

- 📋 **Просмотр задач** – вывод всех активных (не закомментированных) cron-задач с нумерацией.
- ➕ **Добавление задачи** – интерактивный ввод с подробной справкой по формату и специальным значениям (`@reboot`, `@daily` и др.).
- ✏️ **Редактирование задачи** – выбор задачи по номеру и замена её новой.
- 🗑️ **Удаление задачи** – безопасное удаление с подтверждением.
- ✅ **Проверка синтаксиса** – валидация любой cron-строки (поля времени, существование пользователя, наличие команды).
- 📊 **Статус cron-сервиса** – отображение активности, автозапуска и подробного состояния службы.
- 🔐 **Автоматические бэкапы** – перед каждым изменением создаётся копия `/etc/crontab` в `/etc/cron.backups/`.
- 🔄 **Автовосстановление** – при сбое записи скрипт восстановит последний бэкап.

## Защиты и проверки

| Механизм | Описание |
|----------|----------|
| **Резервное копирование** | Перед любым изменением создаётся бэкап с меткой времени. |
| **Запись через временный файл** | Изменения сначала сохраняются во временный файл, затем атомарно заменяют оригинал. |
| **Восстановление при ошибке** | Если запись не удалась, автоматически восстанавливается предыдущая версия. |
| **Проверка синтаксиса** | Валидация полей времени (диапазоны, списки, шаги), существование пользователя, наличие команды. |
| **Поддержка специальных строк** | `@reboot`, `@yearly`, `@monthly`, `@weekly`, `@daily`, `@hourly`. |
| **Подтверждение действий** | Запрос подтверждения перед добавлением, изменением или удалением задачи. |
| **Проверка прав** | При запуске без `root` выводится предупреждение (редактирование требует прав суперпользователя). |
| **Перезагрузка cron** | После успешной записи скрипт пытается перезапустить службу (`cron` или `crond`). |

## Требования

- Linux-система с установленным cron (обычно уже есть).
- Bash 4+.
- Права `sudo` для редактирования `/etc/crontab`.

## Установка

1. **Скопируйте скрипт** в удобную директорию, например `/usr/local/bin/`:
   ```bash
   sudo wget -O /usr/local/bin/cron-manager.sh https://raw.githubusercontent.com/ApaT1aPY/crontab-manager/refs/heads/main/crontab-manager.sh
   ```
2. **Сделайте скрипт исполняемым:**
   ```bash
   sudo chmod +x /usr/local/bin/cron-manager.sh
   ```
3. **(Опционально) Добавьте alias для быстрого вызова:**
   ```bash
   echo "alias crontabmanager='sudo /usr/local/bin/cron-manager.sh'" >> ~/.bashrc
   source ~/.bashrc
   ```
   crontabmanager вы можете заменить на удобное название.

## Использование

1. **Используйте:**
  ```bash
  sudo /usr/local/bin/cron-manager.sh
  ```
  или, если установили alias
  ```bash
  crontabmanager
  ```
2. **Вводите номер пункта и следуйте инструкциям.**
   
   При вводе задач будет показана подробная справка по формату.

## Предпросмотр
<img width="470" height="300" alt="{F34558C9-B1B9-440D-B7E1-A7FDE364ED29}" src="https://github.com/user-attachments/assets/e972e0c7-ecbb-441e-9b07-caede1d345bc" />

